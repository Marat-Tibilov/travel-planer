import {Metadata} from "next";


export const metadata: Metadata = {
    description: 'Favorites',
}

export default function () {
    return(
        <section>
            <p>  </p>
        </section>
    )
}